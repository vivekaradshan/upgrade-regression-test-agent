[pytest]
markers =
    integration: tests that call real external services (e.g. GitHub API)
    e2e: full end-to-end tests exercising the entire orchestrator flow
    evals: LLM diagnosis-quality eval harness (calls the real OpenAI API)
